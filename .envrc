# direnv configuration for automatic environment loading
# Install direnv and run 'direnv allow' to enable

if has nix; then
  if has nix_direnv_version || has nix-direnv-reload; then
    # Use nix-direnv for better caching
    use flake
  else
    # Fallback to regular nix-shell
    use nix
  fi
fi
